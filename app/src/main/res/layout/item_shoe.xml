<!-- res/layout/item_shoe.xml -->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
                name="shoe"
                type="com.udacity.shoestore.models.Shoe"/>
        <variable
                name="shoeListViewModel"
                type="com.udacity.shoestore.ui.shoelist.ShoeStoreViewModel"/>
    </data>

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

        <!-- Example of binding shoeName LiveData -->
        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="School Shoe"
                android:text="@={shoe.name}"
        />

        <!-- Example of binding company LiveData -->
        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Bata"
                android:text="@={shoe.company}"
        />

        <!-- Example of binding shoeSize LiveData -->
        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="9"
                android:text="@{String.valueOf(shoe.size)}"
        />

        <!-- Example of binding description LiveData -->
        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Very comfortable"
                android:text="@={shoe.description}"
        />


    </LinearLayout>
</layout>
